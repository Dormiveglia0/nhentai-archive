# 2026-09-16 设计重校准

## 2026-09-16 · 视觉方案重新校准（未部署）

用户否定了环形首页与六行折叠设置的视觉结果，本轮不将这组截图作为已通过设计。首页已改为真实日期记录的三维阵列，原节点在总览、选中与展开间保留，邻近记录跟随选择抬升；展开时整体取景转正，背景记录退到选中表面之后。设置改为六个配置对象的选择区，选择对象移入编辑区，草稿继续由原状态层维护。全局导航改为与画布连续的浅色表面。

已完成 1440×1000、2560×1440、390×844 布局及连续反向操作检查；动画录像在本地 `/tmp/nh-spatial-video/`。不把这些检查表述为用户认可、参考项目还原或整站重构完成。其他页面的前轮结构仍为待复核稿，后续不能仅靠同色按钮与淡入作为设计交付。

保留阅读器底部悬浮控件与库详情的封面展开。原始远端留档 `2b4fbef201ff6140e3ed7d0878fb5ddbeb60901d`，当前线上仍为 `b553195`；未合并 main。公开远端推送因既有自动审核限制仍未执行。

## 前轮草案（用户否定视觉结果，保留业务核对依据）

# 2026-09-15 全界面重设计

用户确认保留：阅读器底部悬浮控件、库作品详情浮层。其余视觉结构没有继承要求。保留业务能力、验证与数据流，不把旧页面 DOM 或 Folio 面板形态当作新设计约束。

## 设计与功能契约

- 全局：单层水平导航，紧凑标识，内容占据完整宽度。页头独立装饰动画退出正式路由；动画属于被操作对象。
- 首页：真实阅读日期构成可选择的环形时间图，日期、时长、状态及最近作品联动；不再用立体柱形统计面板。
- 登录：独立深色访问界面，中央可开启的几何标识；输入、验证、开启有连续状态，无未登录内容。
- 库：检索与状态索引组成页首，筛选收放，内容模式统一切换。封面以整张图像为主体，标题与元信息形成底部注记；保留浮层详情和原生阅读链接。
- 发现：热门横向开放陈列，排名、图像、作品信息各自对齐；查询区域与结果集合独立。竖图填满，横图完整，稀疏内容不拉伸。
- 设置：六个配置模块直接展开编辑，模块标题与当前配置状态常驻；只有一个内容区展开，草稿留在状态层，保存栏仅负责当前未保存修改。
- 词典：候选结果作为全宽索引，选择后进入编辑浮层；映射、证据及应用操作集中在同一个编辑对象。
- 治理：作品队列为入口，审核文档为主界面，来源折叠到文档旁；字段对比、标签、复核及批量操作均保留。
- 队列：紧凑状态总览、完整任务记录、选中任务的浮出进程与日志。取消重复状态可视化与常驻详情占宽。
- 文件：实际存储占用与分类索引、文件清单、详情浮层；扫描/删除仍需原预览与确认。
- 导出：选择作品、配置输出的两步工作区；状态筛选、多选、警告与预览、命名、打包及单独下载全部可达。
- 历史：按日期分区的阅读清单，日期与记录共同排版，不再额外常驻目录。
- 远端详情：封面、作品资料、页面索引形成阅读前的完整打开状态；保留导入、标签、关联和灯箱。

## 运动规则

同一选择对象的焦点与表面响应一致。展开从触发对象的位置开始，反向中断不跳回初始值。列表轮询不重播入场动画。只在可见、前台时播放待机动效，减少动态效果保留全部功能。共享现有 Motion 与原生 dialog，避免引入第二个动画引擎。

## 验收

1440×1000、2560×1440、390×844实际浏览器检查；实数据隔离副本验证。覆盖导航、筛选、阅读、浮层关闭与焦点、设置草稿、队列筛选、文件删除取消、导出预览和词典草稿。截图检查与交互检查不能互相替代。不上 main，旧版远端留档 2b4fbef；公开仓库推送仍待明确授权。
